# Installed-VSIX export smoke tests

This harness exercises installed local desktop VS Code on macOS, Linux or Windows: toolbar, actual save lifecycle, installed converters, cancellation and output filenames. It complements the artifact inspection tools in this directory; passing it does not certify visual fidelity or environments absent from its receipts. Follow [0.2.0 integration validation](../../reports/validation/0.2.0.md) for current package evidence; the [export-branch record](../../reports/validation/2026-09-10-export.md) preserves earlier Ubuntu x86-64/macOS ARM64 observations.

The consolidated `--suite all` run passed on 2026-09-09 in a fresh isolated installation: **39 scenario receipts plus a final frozen-input check**. That run used VS Code 1.136.0, Node 20.20.0, Pandoc 3.8.3 and Google Chrome 152.0.7977.65. The VSIX was 4,187,330 bytes, SHA-256 `958ba238a2b064888c9b5e76631c2aa884004e4769ade61c72523168c4808438`. Re-run after changing the implementation or environment; this is a dated receipt, not a compatibility guarantee.

## Prerequisites

- Use the Node version in `.node-version`, local desktop VS Code on macOS, Linux or Windows and the VS Code `code` CLI. Supply `--code /path/to/code` when it is not on PATH. On Windows, pass the isolated archive’s absolute `Code.exe` path; the harness uses its packaged CLI without a command-shell wrapper. Run as an ordinary user, not root.
- Run `npm ci`, `npm run package` and `python3 test/fixtures/exports/verify-fixtures.py` from the checkout.
- Install Pandoc and Chrome/Chromium/Edge yourself before the full four-format run. No command here downloads native engines. Optional `--pandoc /path/to/pandoc` and `--browser /path/to/browser` select manual executable paths; empty defaults exercise automatic discovery.
- Close the earlier test window before starting another session on the same port. Normal VS Code windows can remain open. Linux needs a local desktop display or an explicitly provisioned Xvfb display; SSH alone is not a display. This tests a local extension host, not VS Code Remote-SSH.

Frozen fixture files remain byte-identical throughout the run. When a fixture contains an unexpanded TOC, the format/offline suites open a derived copy beside it, refresh through the installed editor and save before exporting. The harness compares the saved copy against the exact expected TOC-only change, records original/saved hashes, and checks that conversion leaves the saved copy unchanged.

## Create and run an isolated test installation

Run these commands from the checkout. The defaults use `.vscode-test/export-native-repro` and debugging port `9327`:

```sh
node test/native/export-smoke.cjs init
node test/native/export-smoke.cjs install
node test/native/export-smoke.cjs launch
```

Initialization requires a **new directory** and never erases existing files. It copies the frozen and supplementary fixtures and creates owned extensions/workspace directories beneath the ignored `.vscode-test` directory. The profile is a new `bm-native-*` directory under the canonical system temporary directory. IPC socket paths must stay short: use `TMPDIR=/tmp` when necessary and keep it consistent for every command. Matching ownership sentinels bind the workspace, temporary profile and installation to one random token; their paths are printed at initialization and recorded in the sentinel. Choose another dedicated directory with `--workdir .vscode-test/export-native-second` for another clean run; pass the same value on every command.

If VS Code asks for Workspace Trust, inspect the generated workspace path and trust **only that test workspace**. The driver refuses a missing sentinel, a different workspace/profile, or a development checkout in place of the isolated installed extension. It cannot issue arbitrary VS Code commands or modify normal-profile settings. In the recorded automated run, trust was disabled only in the newly generated temporary test profile; no normal-profile setting was changed.

```sh
node test/native/export-smoke.cjs check
node test/native/export-smoke.cjs run --suite all
```

`check` waits for a live inspection response from the guarded driver and verifies the seven frozen input hashes before reporting ready. `run` repeats ownership and input guards and requires the package hash recorded by `install` before making changes. The window title includes a random ownership token; the CDP connection selects that window's **visible editor iframe and active inner frame**, then every successful export must have the selected document's filename stem and sibling directory. CDP frame ownership identifies the active inner frame even on older Electron where JavaScript cannot access `window.frameElement`. Background editors and the temporary pending frame used during reloads cannot satisfy those assertions.

The dev-only `native-playwright` alias attaches with [`noDefaults`](https://playwright.dev/docs/api/class-browsertype#browser-type-connect-over-cdp-option-no-defaults), preserving Electron's download, focus and media defaults. This avoids an unsupported `Browser.setDownloadBehavior` request on VS Code 1.85.0. It does not replace the shipped PDF controller. Portable Linux VS Code archives can require their normal sandbox helper to be installed correctly; the disposable CI provisioner configures that helper instead of disabling the sandbox. Ordinary installed VS Code and a disposable CI archive have different setup requirements.

Use `--suite document-aux` for metadata preservation, manual TOC refresh, native/keyboard saves in both modes, repeated-save stability and real exports.

Use `--suite links` for absolute and workspace-relative directory links, absolute files, encoded file URIs, destination tooltips, unavailable-target copying, and unchanged Markdown. The clipboard check restores its original contents and records only a comparison result. This suite is included in `--suite all`.

Use `--suite codeblocks` for indented fences, inactive highlighting restoration, quoted-code colors, the installed source-mode keyboard shortcut immediately after typing, and native save/reopen preservation of quoted code. This suite uses synthetic content and is included in `all`.

Use `--suite blockquotes` for measured quote contrast across all seven themes, live theme changes with retained selection and undo, unchanged source, and actual HTML/PDF exports. For the Electron development app, install dependencies in `electron/`, compile the root and run `npx tsc -p electron`, then run `node test/native/electron-blockquotes.cjs` from the repository root. That smoke check opens the real Preferences window using a new isolated profile, retains screenshots and a receipt under `.vscode-test/`, and closes only its own application. Neither command establishes compatibility on hosts absent from its receipt.

Use `--suite table-placement` for all eight placements selected once through the installed picker at user and workspace scope, rapid choices, retained selection, reopening, and table action/undo checks. It also constrains the installed editor frame to exercise horizontal and vertical overflow, complete visible buttons, all actions remaining reachable, open-menu resizing, Escape, and one-step undo. This is a frame-resize probe; actual OS-window resizing remains a manual check because VS Code's debugging interface does not expose that command. Receipts record the observed webview dimensions. The guarded driver permits workspace writes only for toolbar placement, toolbar mode, and editor layout preferences within its owned workspace; the focused suites restore their overrides afterward.

The installed VS Code probe activates visible controls through their production DOM handlers and sends keyboard input to the owned workbench. Browser and Electron checks cover pointer activation separately; the VS Code receipt does not establish native pointer hit testing.

Use `--suite text-toolbar` to check the packaged Full default with no preference, explicit user/workspace overrides, live selection preservation, undo after mode changes, and persistence after reopening. Layout checks constrain the installed webview frame; OS window dragging remains manual. After compiling the root and Electron, `node test/native/electron-text-toolbar.cjs` checks unset/Simple/Full startup in isolated desktop profiles, real Preferences controls and IPC, a simulated settings-save failure and retry, and actual Electron resizing at 100% and 200% zoom. It opens the real Preferences window directly through its application method, bypassing the OS menu's focused-window requirement; it does not claim OS-menu activation or spoken screen-reader coverage. Receipts and synthetic captures are retained under `.vscode-test/`.

Use `--suite table-content` for local scrolling of a wide table, forward/backward navigation through every header and body cell, caret visibility after pane resizing, unchanged source and undo state, and native save/reopen after an unrelated paragraph edit. It uses the same installed-frame resize boundary described above. After building the root and Electron, `node test/native/electron-table-content.cjs` runs the corresponding checks with actual Electron window resizing, saving and reopening an isolated synthetic document. It retains light/dark captures at 100% and 200% zoom and a receipt under `.vscode-test/`. These checks do not establish spoken screen-reader behavior or add support for table syntax that the parser does not already recognize.

Use `--suite table-row` to check Full/Simple contextual rows with Automatic, top-bar and floating placements, complete buttons, viewport bounds, keyboard entry/Escape, and a table action followed by a mode change and one undo. It uses installed-frame resizing and checks Source mode and native save/reopen. After compiling both hosts, `node test/native/electron-table-row.cjs` checks the same row geometry through real desktop Preferences and window resizing, retaining four captures and a receipt under `.vscode-test/`. It opens Preferences directly as described for `text-toolbar`; OS-menu activation and spoken screen-reader behavior remain manual checks.

Use `--suite insert-menu` for the eight Insert actions, Full/Simple menu navigation, retained selection, disabled block contexts, single-step undo/redo, Source mode, and native save/reopen. Link checks use real VS Code input boxes, including Escape from the second prompt. Image checks temporarily enable VS Code's simplified file picker in the owned profile, then restore its previous setting. This covers the actual file-picker command, image copying, host response, and insertion; it does not exercise the operating system's native file dialog.

After compiling both hosts, `node test/native/electron-insert-menu.cjs` exercises the same insertions and undo checks through the desktop app, its real link form, and Preferences controls. The image test supplies a controlled OS-picker result and runs the real IPC, file-copy, insertion, and undo paths. The receipt explicitly excludes OS-picker interaction. It retains synthetic captures and evidence under `.vscode-test/`, checks Source mode and saving, and closes only its owned application. Spoken screen-reader behavior remains a manual check.

Use `--suite underline` for the shared underline command in paragraphs, nested bold text, lists, table cells, and quotes; it checks the native keyboard shortcut, saving each change, undo/redo, literal code, Source mode, and reopening. After compiling both hosts, `node test/native/electron-underline.cjs` runs the corresponding checks in an isolated desktop profile. These checks establish editor/source behavior; they do not establish underline support in export readers.

Use `--suite underline-exports` for the supplementary `underline-export.md` fixture. It verifies save/reopen, all four installed conversion routes, native DOCX underline runs, HTML/EPUB markup, PDF text, literal examples, and unchanged source/editor state. It requires the actual converters and `pdftotext` described above. These are structural and conversion checks; inspect the resulting files in representative readers to assess appearance and editing.

Use `--suite editor-width` for real user/workspace settings across Default, Full width, and Custom modes. It checks measured column bounds, selection and node retention, clean state, undo, native saving, Source layout, and preference persistence after reopening. Frame measurements are installed-host evidence; actual OS window dragging and spoken accessibility remain separate manual checks. After root and Electron compilation, run `node test/native/electron-editor-width.cjs` for real Preferences controls, invalid input, save failure/retry, native save, new-editor persistence, and actual Electron window sizes at 100% and 200% zoom. It uses an isolated profile and synthetic document, opening Preferences directly as described for `text-toolbar`.

Use `--suite editor-alignment` for real user/workspace alignment changes, measured column placement, unchanged paragraph/table alignment, retained selection and active equation input, table-control bounds, undo, native save, Source mode, and reopening. After compiling both hosts, `node test/native/electron-editor-alignment.cjs` covers real Preferences controls, a failed-save retry, source/undo preservation, new-editor persistence, and actual resizing at 100% and 200% zoom in an isolated desktop profile. It uses the same direct Preferences-opening and native-interaction boundaries described above.

Use `--suite width-indicators` for the installed user/workspace hide setting, alignment and boundary geometry, focus-accessible explanation, unchanged document/undo, native save, Source mode, and reopening. After root and Electron compilation, `node test/native/electron-width-indicators.cjs` exercises real Preferences controls, failed-save rollback/retry, persistence, and real window resizing at 100% and 200% zoom. It shares the isolated-profile and direct Preferences-opening boundaries above. Browser coverage additionally checks threshold stability, hover/Escape, theme colors, selection, and export exclusion.

Use `--suite language-picker` for autofocus, readable-name/alias searches, custom-language preservation, empty results, keyboard confirmation/Escape, one-step undo/redo, native save, Source mode, and reopening. After compiling both hosts, `node test/native/electron-language-picker.cjs` additionally checks real clipboard content and window resizing at 100%/200% zoom in an isolated desktop profile. Browser coverage also checks caret restoration, no-op choices, Tab navigation, and intentional Math/Mermaid conversion with preserved trailing blank lines. Native VS Code retains the DOM-activation/owned-keyboard boundary described above.

For Electron table checks, build the root and Electron as above, then run `node test/native/electron-table-placement.cjs`. This checks the real Preferences window and editor picker, settings persistence, an injected write failure and retry, retained selection/undo, and the same overflow checks through actual window resizing. It requires an unlocked desktop for native menu focus. `--picker-only` exercises the editor picker and its real IPC/storage path without the Preferences window; its receipt explicitly excludes Preferences coverage. Both modes isolate settings, retain a receipt under `.vscode-test/`, and close their own app.

The code-block suite waits for the source round trip to reach the host before beginning its separate quoted-code save scenario; Save remains immediate after that scenario's new paragraph edit. Overlapping an earlier queued host edit with native save preparation is a separate, reproduced baseline defect tracked in [issue #11](https://github.com/BinaryOutlook/binary-markdown/issues/11). This suite does not certify that overlap behavior.

Use `--suite formats`, `saves`, `edges`, `ui`, `equations`, `pdf-background`, `selection`, `immutable` or `offline` for a focused run. The package default is `dist/<package-name>-<package-version>.vsix` from the checkout's manifest. `--package dist/binary-markdown-0.2.0.vsix`, `--port 9327`, `--workdir ...`, `--code ...` and `--timeout 120000` are explicit options. Keep the initialization port on subsequent commands. The timeout is a test watchdog, not a product timing requirement; increase it for a complex report or slower machine. In an environment that sandboxes tool execution, native VS Code and browser/CDP access need the environment's explicit native-process permission.

The `identity` suite, included in full runs, checks **Copy Build Information** against the installed source stamp and restores the previous clipboard. The historical 39-scenario receipt above predates repeated configuration cycles, build-information and equation coverage. Current suites cover:

- **Formats:** frozen basic/fallback/pagination/W-30 sources through HTML, PDF, DOCX and EPUB; output identity and unchanged source bytes.
- **Saves:** immediate native Save and Command-S on macOS or Ctrl-S on Linux in both visual/source modes; exported marker, unchanged saved source and editor state.
- **Edges:** missing engines with independent HTML, settings without editor reload, output-byte hash/reuse, dirty/untitled rejection, unwritable output, a held referenced image with editing/cancellation, and a controlled test-only Pandoc worker for cancellation/failure. The worker uses the Node executable running the harness rather than a hard-coded personal path.
- **UI:** three full/Chinese → simple/English configuration cycles with toolbar placement and keyboard navigation, runtime labels independent of native English VS Code, and GitHub/night HTML exports. The harness reconnects to the active visible editor only after `data-export-ready="true"`, then checks the requested toolbar mode and label; static English markup alone is not readiness. It brings the owned window forward, verifies real button focus, and checks keydown/keyup pairs and observable menu state.
- **Equations:** ten inline/display equations using dollar, backslash and fenced syntax, including aligned rows, a matrix, quotes, a table and a nested list. Checks rendering, source mode, native Save while an equation input is active, all four exports, native DOCX/EPUB math counts and unchanged saved source. Disables backslash recognition and verifies matching editor/DOCX behavior. Requires Poppler `pdftotext` and `unzip` on PATH.
- **PDF background:** default-white, explicit theme and explicit-white exports from a night editor; actual multi-page A4 rasters, text/diagram labels, unchanged source/editor, live setting changes without reload, and HTML theme isolation. Requires Poppler `pdftotext` and `pdftoppm` on PATH (or `EXPORT_PDFTOTEXT_PATH` / `EXPORT_PDFTOPPM_PATH`).
- **Selection:** a normal text tab cannot export a hidden Markdown editor.
- **Immutable:** a held referenced resource allows later unsaved edits while the successful export preserves its earlier saved capture.
- **Offline:** four relocated HTML exports load offline without external requests, script errors or editor controls; all images decode and the basic original image bytes are preserved. A focused offline run first creates its own four HTML files through the installed extension.

Reports are timestamped JSON under the owned directory's `evidence/`; relocated HTML and its basic screenshot are retained there too. The recorded run used `.vscode-test/export-native-consolidated/evidence/native-receipt.json`, with engine details in `tools.json`. Preserve the report with the tested VSIX hash, converter versions and artifact inspection results. Repackage/reinstall after implementation changes; the old installed VSIX is not automatically refreshed. Run the separate `EXPORT_VSIX_PATH=... node --test test/unit/export-package.test.js` gate to check the archive against the compiled checkout. Syntax checks and rejected missing-receipt, outside-workspace and duplicate-initialization guards also passed.

The CI wrapper additionally retains native host logs, a failure screenshot and named watchdog observations. It closes only its sentinel-bound test process, keeps the profile/evidence, and never targets normal VS Code windows.

When running the harness directly, close only the uniquely titled test window when finished. The harness has no cleanup command and never kills general VS Code processes or deletes profiles. Retain or manually remove only the specific owned `.vscode-test` directory and temporary profile recorded in its sentinel when their evidence is no longer needed.
