# Renderer-native mathematics

RENDERER-MATH-FIRST: This supplementary fixture exercises the existing fenced math renderer. It does not replace or modify the frozen export reference inputs.

```math
\frac{a^2+b^2}{c^2}=1
```

The next supported block contains a summation and a fraction.

```math
\sum_{k=1}^{n} k = \frac{n(n+1)}{2}
```

RENDERER-MATH-LAST: Both equations should retain their rendered form and required KaTeX fonts in standalone HTML.
